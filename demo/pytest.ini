[pytest]
addopts = -v --tb=short -W default::DeprecationWarning
markers =
    run: Mark a test to be executed in a specific order
    oss2: Tests for OSS2 backend system
    scms: Tests for SCMS supplier system
filterwarnings =
    ignore::DeprecationWarning
